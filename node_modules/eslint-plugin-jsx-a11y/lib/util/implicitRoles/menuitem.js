

Object.defineProperty(exports, '__esModule', {
  value: true,
});
exports.default = getImplicitRoleForMenuitem;

const _jsxAstUtils = require('jsx-ast-utils');

/**
 * Returns the implicit role for a menuitem tag.
 */
function getImplicitRoleForMenuitem(attributes) {
  const type = (0, _jsxAstUtils.getProp)(attributes, 'type');

  if (type) {
    const value = (0, _jsxAstUtils.getLiteralPropValue)(type) || '';

    switch (value.toUpperCase()) {
      case 'COMMAND':
        return 'menuitem';
      case 'CHECKBOX':
        return 'menuitemcheckbox';
      case 'RADIO':
        return 'menuitemradio';
      default:
        return '';
    }
  }

  return '';
}
