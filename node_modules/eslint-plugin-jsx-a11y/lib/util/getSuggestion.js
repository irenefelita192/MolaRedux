

Object.defineProperty(exports, '__esModule', {
  value: true,
});
exports.default = getSuggestion;

const _damerauLevenshtein = require('damerau-levenshtein');

const _damerauLevenshtein2 = _interopRequireDefault(_damerauLevenshtein);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// Minimum edit distance to be considered a good suggestion.
const THRESHOLD = 2;

/**
 * Returns an array of suggestions given a word and a dictionary and limit of suggestions
 * to return.
 */
function getSuggestion(word) {
  const dictionary = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];
  const limit = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 2;

  const distances = dictionary.reduce((suggestions, dictionaryWord) => {
    const distance = (0, _damerauLevenshtein2.default)(word.toUpperCase(), dictionaryWord.toUpperCase());
    const steps = distance.steps;

    suggestions[dictionaryWord] = steps; // eslint-disable-line
    return suggestions;
  }, {});

  return Object.keys(distances).filter(suggestion => distances[suggestion] <= THRESHOLD).sort((a, b) => distances[a] - distances[b]) // Sort by distance
    .slice(0, limit);
}
